# Installation instructions

```bash
pip install omop-llm
```

Structured extraction via `instructor` (see [docs/index.md](../index.md)) is an optional extra:

```bash
pip install "omop-llm[instructor]"
```
